#include <stdio.h>
int main()
{
    int n =0, factorial =0;
    printf("Enter a value:");
    scanf("%d", &n);

    for (int i =1,  j = 1, k =2; i <= n; i++, j ++, k++ ) {

        //printf("Loop iteration number:%d ", i); i tracks loop iteration
        //printf("Repeated number:%d", j ); j tracks the series
        //printf("Repeated number:%d\n", k ); k tracks the series
        factorial = sum + j*k;

    }
        printf("%d ", sum);
}

